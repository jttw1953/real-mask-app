export default function GuestNavbar() {
  return (
    <nav className="fixed inset-x-0 top-0 z-50 h-14 border-b border-white/10 bg-slate-900/70 backdrop-blur">
      <div className="mx-auto flex h-full w-full max-w-6xl items-center px-4">
        <div className="text-slate-200 text-lg font-semibold tracking-wide">
          Real Mask
        </div>
      </div>
    </nav>
  );
}
